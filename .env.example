# Example environment variables (DO NOT put secrets here)

# Default voice (persona). VoiceMonkey must support the name in your account.
VM_DEFAULT_VOICE=Camila

# Default language. Examples: pt-BR, en-US, es-ES
VM_DEFAULT_LANGUAGE=pt-BR

# Optional: change bind/port if you know what you're doing.
# VM_PROXY_HOST=127.0.0.1
# VM_PROXY_PORT=18793
